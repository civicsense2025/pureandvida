{%- liquid
  # @description Renders an arrow icon SVG (right-pointing arrow)
  # @param class {String} - Additional CSS classes (optional)
  # @param size {Number} - Icon size in pixels (optional, default: 14)
  # @example
  #   {% render 'icon-arrow', class: 'button-icon', size: 16 %}
-%}
<svg aria-hidden="true" focusable="false" class="icon icon-arrow{% if class != blank %} {{ class }}{% endif %}" viewBox="0 0 14 10" fill="none"{% if size != blank %} width="{{ size }}" height="{{ size }}"{% endif %}>
  <path fill-rule="evenodd" clip-rule="evenodd" d="M8.537.808a.5.5 0 01.817-.162l4 4a.5.5 0 010 .708l-4 4a.5.5 0 11-.708-.708L11.293 5.5H1a.5.5 0 010-1h10.293L8.646 1.354a.5.5 0 01-.109-.546z" fill="currentColor"/>
</svg>
